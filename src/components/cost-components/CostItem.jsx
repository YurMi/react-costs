import './cost-item.css';import CostDate from "./CostDate";export default function CostItem(props) {    const {date,name,price} = props    return (        <li className={'cost_item box-wrapper'}>            <CostDate date={date}/>            <div className="cost_item__info">                <h3 className="cost_item__name">{name}</h3>                <span className="cost_item__price">${price}</span>            </div>        </li>    )}