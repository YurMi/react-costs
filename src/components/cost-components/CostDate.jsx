import './cost-date.css'export default function CostDate(props){    const {date} = props;    const thisMonth = date.toLocaleString('en-En',        {month: 'long'}    );    const thisDate = date.getDate();    const thisYear = date.getFullYear();    console.log()    return (        <div className="cost_item__date date box-wrapper">            <p className="date__month">{thisMonth}</p>            <p className="date__number">{thisDate}</p>            <p className="date__year">{thisYear}</p>        </div>    )}