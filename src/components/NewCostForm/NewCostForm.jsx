import './new-cost-form.css'import {useState} from "react";export default function NewCostForm(props) {	const {addNewCostHandler, hiddenForm} = props	const [costName, setCostName] = useState('');	const [costPrice, setCostPrice] = useState('');	const [costDate, setCostDate] = useState('');	const saveCostName = (e) => setCostName(e.target.value)	const saveCostPrice = (e) => setCostPrice(e.target.value)	const saveCostDate = (e) => setCostDate(e.target.value)	const submitHandler = (e) => {		e.preventDefault();		const newCostObj = {			id: Date.now(),			date: costDate ? new Date(costDate) : '',			name: costName,			price: costPrice,		}		const hasEmptyValues = Object.values(newCostObj).some(value => !value)		if (hasEmptyValues) {			alert("You have Empty field")		} else {			addNewCostHandler(newCostObj);			setCostName('');			setCostPrice('');			setCostDate('');		}	}	return (		<form			className={'new_cost_form box-wrapper'}			onSubmit={submitHandler}		>			<div className="new_cost_form__body">				<div>					<label htmlFor='fname'>Cost Name</label>					<input						value={costName}						type="text"						id='fname'						onChange={saveCostName}					/>				</div>				<div>					<label htmlFor='fdate'>Cost Price</label>					<input						value={costPrice}						type="number"						id='fdate'						onChange={saveCostPrice}					/>				</div>				<div>					<label htmlFor='fdate'>Date</label>					<input						value={costDate}						type="date"						id='fdate'						onChange={saveCostDate}					/>				</div>			</div>			<div className="new_cost_form__buttons">				<button type="submit">Add Cost</button>				<button type='button' className='cansel' onClick={hiddenForm}>Cansel</button>			</div>		</form>	)}